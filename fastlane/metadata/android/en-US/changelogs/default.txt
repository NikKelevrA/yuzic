Fixes a crash when Yuzic scanned a large Jellyfin library.

- The library is fetched a page at a time now. It used to be asked for in one request, and on a big library that single response was large enough to run the app out of memory before it could draw anything
- Sorting a large library no longer rebuilds the whole list first
