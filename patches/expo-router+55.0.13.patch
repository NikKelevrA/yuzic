diff --git a/node_modules/expo-router/build/ExpoRoot.js b/node_modules/expo-router/build/ExpoRoot.js
index e49b55b..69f8276 100644
--- a/node_modules/expo-router/build/ExpoRoot.js
+++ b/node_modules/expo-router/build/ExpoRoot.js
@@ -55,12 +55,18 @@ const Sitemap_1 = require("./views/Sitemap");
 const SplashScreen = __importStar(require("./views/Splash"));
 const Unmatched_1 = require("./views/Unmatched");
 const isTestEnv = process.env.NODE_ENV === 'test';
+const ZERO_METRICS = {
+    frame: { x: 0, y: 0, width: 0, height: 0 },
+    insets: { top: 0, left: 0, right: 0, bottom: 0 },
+};
+// yuzic: a car can launch the app into its CarPlay scene with no phone window.
+// SafeAreaProvider renders nothing until native insets arrive, and with no
+// window they never do, so the whole app, the car's library included, stayed
+// unmounted. The key window's metrics when there is one, as before; zero
+// until the real ones arrive when there is not.
 const INITIAL_METRICS = react_native_1.Platform.OS === 'web' || isTestEnv
-    ? {
-        frame: { x: 0, y: 0, width: 0, height: 0 },
-        insets: { top: 0, left: 0, right: 0, bottom: 0 },
-    }
-    : undefined;
+    ? ZERO_METRICS
+    : (require('react-native-safe-area-context').initialWindowMetrics ?? ZERO_METRICS);
 const documentTitle = {
     enabled: false,
 };
