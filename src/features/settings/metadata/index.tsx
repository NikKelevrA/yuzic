import React from 'react';

import SourcePurposeScreen from '../sources/SourcePurposeScreen';

/** Fills in what your server is missing: artist info, artwork and lyrics. */
export default function MetadataSettings() {
  return <SourcePurposeScreen screen="metadata" />;
}
